package com.ai.basewebsocket.handler;

import org.springframework.stereotype.Component;

/**
 * @Description
 * @Author 
 * 
 */
@Component
public class TestMsgHandler {


/*    public boolean sendToUser(WsToUserParam param) throws Exception{
        WebSocketDTO webSocketDTO = new WebSocketDTO();
        webSocketDTO.setCode(param.getCode());
        webSocketDTO.setContent(param.getContent());
        WebSocketUserServer.sendUser(param.getOpenid(),webSocketDTO);
        return true;
    }*/


/*    public boolean sendToMarket(WsToMarketParam param) throws Exception{
        WebSocketDTO webSocketDTO = new WebSocketDTO();
        webSocketDTO.setCode(param.getCode());
        webSocketDTO.setContent(param.getContent());
        WebSocketMarketServer.sendMarket(param.getMarket(),webSocketDTO);
        return true;
    }*/


/*    public boolean sendToCommon(WsToCommonParam param) throws Exception{
        WebSocketDTO webSocketDTO = new WebSocketDTO();
        webSocketDTO.setCode(param.getCode());
        webSocketDTO.setContent(param.getContent());
        WebSocketServer.sendAll(webSocketDTO);
        return true;
    }*/



}
