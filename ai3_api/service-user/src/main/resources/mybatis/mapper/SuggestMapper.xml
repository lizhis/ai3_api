<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ai.serviceuser.mapper.SuggestMapper">

	<resultMap id="BaseResultMap" type="com.ai.basecommon.core.po.user.SuggestPO">
		<result column="id" jdbcType="BIGINT" property="id" />
		<result column="user_id" jdbcType="BIGINT" property="userId" />
		<result column="content" jdbcType="VARCHAR" property="content" />
		<result column="images" jdbcType="VARCHAR" property="images" />
		<result column="reply" jdbcType="VARCHAR" property="reply" />
		<result column="status" jdbcType="INTEGER" property="status" />
		<result column="create_time" jdbcType="BIGINT" property="createTime" />
		<result column="update_time" jdbcType="BIGINT" property="updateTime" />
	</resultMap>

	<insert id="insert">
		insert into tw_suggest (user_id,content,images,create_time,update_time)
		values (#{userId},#{content},#{images},#{createTime},#{updateTime})
	</insert>


	<select id="lastReply" resultType="String">
		select reply from tw_suggest where user_id = #{userId} and reply is not null order by id desc limit 1
	</select>


</mapper>
